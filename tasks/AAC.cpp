#include <bits/stdc++.h>
using namespace std;

class AAC {
public:
    void solve(std::istream& cin, std::ostream& cout) {
		ios::sync_with_stdio(false);
        cin.tie(nullptr);

        string s;
        cin >> s;

        if (s == "ABC"){
            cout << "ARC" ;
        }else{
            cout << "ABC" << endl;
        }
	}
};
