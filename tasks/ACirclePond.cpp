#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

class ACirclePond {
public:
    void solve(std::istream& cin, std::ostream& cout) {
		ios::sync_with_stdio(false);
        cin.tie(nullptr);
	}
};
