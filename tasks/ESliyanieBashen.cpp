#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
#define all(x) x.begin(),x.end()
class ESliyanieBashen {
public:
    void solve(std::istream& cin, std::ostream& cout) {
		ios::sync_with_stdio(false);
        cin.tie(nullptr);
        int n,m;

        cin >> n >> m;

        vector<vector<int>> a(m);

        for (int i = 0; i < n; ++i) {
            int x;
            cin >> x;
            a[x].push_back(i);
        }


        


	}
};
